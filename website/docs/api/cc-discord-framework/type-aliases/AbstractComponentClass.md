# 型エイリアス: AbstractComponentClass\<T\>

```ts
type AbstractComponentClass<T> = () => T;
```

定義: src/component/metadata.ts:30

抽象クラスも許容するコンポーネントクラス型 — デコレータが受け取る型。

## 型パラメーター

### T

`T` *extends* [`Component`](../classes/Component.md) = [`Component`](../classes/Component.md)

## 戻り値

`T`
